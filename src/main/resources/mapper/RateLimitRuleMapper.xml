<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.lulucar.blog.mapper.RateLimitRuleMapper">
    <select id="findAll" resultType="cn.lulucar.blog.entity.RateLimitRule">
        SELECT id, dimension, capacity, refill_rate
        FROM rate_limit_rule
    </select>
</mapper>